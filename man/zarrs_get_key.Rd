% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{zarrs_get_key}
\alias{zarrs_get_key}
\title{Read a single key from a store as raw bytes.}
\usage{
zarrs_get_key(store_url, key)
}
\arguments{
\item{store_url}{Filesystem path or URL to the store root.}

\item{key}{Key within the store (e.g. \code{".zmetadata"} or \code{"lat/.zattrs"}).}
}
\description{
Open (or reuse) the store at \code{store_url} and fetch \code{key} verbatim.
Returns a raw vector, or \code{NULL} when the key is absent. This is the
key-level read that cloud stores otherwise lack: \code{S3Store} and
\code{GcsStore} delegate all I/O to \code{object_store}, so consolidated
metadata (\code{.zmetadata}) is unreachable from R without it.
}
\details{
Bytes are returned unparsed. Callers decode as needed --- metadata
keys are UTF-8 JSON, chunk keys are compressed binary.
}
\section{Arguments}{
\itemize{
\item \code{store_url} - Filesystem path or URL to the store root.
\item \code{key} - Key within the store (e.g. \code{".zmetadata"} or \code{"lat/.zattrs"}).
}
}

\section{Errors}{
Returns an R error if \code{key} is not a valid store key, if the store
cannot be opened, or if a storage I/O error occurs.
}

